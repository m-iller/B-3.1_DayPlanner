<UserControl x:Class="DayPlannerApp.Views.DayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DayPlannerApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.Resources>
        <local:DeadlineProximityConverter x:Key="DeadlineProximityConverter"/>
        <local:DeadlineProximityTextConverter x:Key="DeadlineProximityTextConverter"/>
    </UserControl.Resources>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <TextBlock Grid.Row="0" 
                  Text="Day View" 
                  FontSize="28" 
                  FontWeight="Bold" 
                  Margin="0,0,0,15"/>
        
        <!-- Date Navigation -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <Button Grid.Column="0" 
                   Content="◀ Previous" 
                   Command="{Binding NavigateToPreviousDayCommand}" 
                   Width="100" 
                   Height="35"
                   Margin="0,0,10,0"/>
            
            <Border Grid.Column="1" 
                   BorderBrush="#2196F3" 
                   BorderThickness="2" 
                   CornerRadius="5"
                   Background="#E3F2FD"
                   Padding="15,8">
                <TextBlock Text="{Binding SelectedDate, StringFormat='dddd, MMMM dd, yyyy'}" 
                          FontSize="18" 
                          FontWeight="SemiBold"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"/>
            </Border>
            
            <Button Grid.Column="2" 
                   Content="Today" 
                   Command="{Binding NavigateToTodayCommand}" 
                   Width="80" 
                   Height="35"
                   Margin="10,0"/>
            
            <Button Grid.Column="3" 
                   Content="Next ▶" 
                   Command="{Binding NavigateToNextDayCommand}" 
                   Width="100" 
                   Height="35"
                   Margin="10,0,0,0"/>
        </Grid>
        
        <!-- Tasks for Day -->
        <Border Grid.Row="2" 
               BorderBrush="#E0E0E0" 
               BorderThickness="1" 
               CornerRadius="5"
               Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" 
                          Text="{Binding TasksForDay.Count, StringFormat='Tasks: {0}'}" 
                          FontSize="16" 
                          FontWeight="SemiBold"
                          Margin="0,0,0,10"/>
                
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding TasksForDay}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#E0E0E0" 
                                       BorderThickness="1" 
                                       CornerRadius="5" 
                                       Padding="15" 
                                       Margin="0,0,0,10"
                                       Background="White">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="5" 
                                                         ShadowDepth="2" 
                                                         Opacity="0.2"/>
                                    </Border.Effect>
                                    
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        
                                        <!-- Header with Type, Time, and Proximity -->
                                        <Grid Grid.Row="0" Margin="0,0,0,10">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <!-- Type Badge -->
                                            <Border Grid.Column="0" 
                                                   Width="50" 
                                                   Height="25" 
                                                   CornerRadius="3" 
                                                   Margin="0,0,10,0">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding TaskTypeId}" Value="1">
                                                                <Setter Property="Background" Value="#4CAF50"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding TaskTypeId}" Value="2">
                                                                <Setter Property="Background" Value="#2196F3"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding TaskTypeId}" Value="3">
                                                                <Setter Property="Background" Value="#FF9800"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding TaskTypeId}" Value="4">
                                                                <Setter Property="Background" Value="#F44336"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding TaskTypeId}" 
                                                          Foreground="White" 
                                                          HorizontalAlignment="Center" 
                                                          VerticalAlignment="Center"
                                                          FontWeight="Bold"/>
                                            </Border>
                                            
                                            <!-- Deadline Proximity Indicator -->
                                            <Border Grid.Column="1" 
                                                   Background="{Binding DeadlineDate, Converter={StaticResource DeadlineProximityConverter}}"
                                                   CornerRadius="3"
                                                   Padding="8,4"
                                                   HorizontalAlignment="Center">
                                                <TextBlock Text="{Binding DeadlineDate, Converter={StaticResource DeadlineProximityTextConverter}}" 
                                                          Foreground="White"
                                                          FontSize="11"
                                                          FontWeight="Bold"/>
                                            </Border>
                                            
                                            <!-- Urgency -->
                                            <StackPanel Grid.Column="2" Orientation="Horizontal">
                                                <TextBlock Text="Urgency: " 
                                                          FontWeight="SemiBold" 
                                                          Margin="0,0,5,0"/>
                                                <TextBlock Text="{Binding UrgencyLevel}" 
                                                          FontWeight="Bold"
                                                          Foreground="#F44336"/>
                                            </StackPanel>
                                        </Grid>
                                        
                                        <!-- Description -->
                                        <TextBlock Grid.Row="1" 
                                                  Text="{Binding Description}" 
                                                  TextWrapping="Wrap" 
                                                  FontSize="14"
                                                  Margin="0,0,0,10"/>
                                        
                                        <!-- Footer with Tags and Deadline Time -->
                                        <Grid Grid.Row="2">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <!-- Tags -->
                                            <ItemsControl Grid.Column="0" ItemsSource="{Binding Tags}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <WrapPanel/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Background="#E0E0E0" 
                                                               CornerRadius="3" 
                                                               Padding="6,3" 
                                                               Margin="0,0,5,0">
                                                            <TextBlock Text="{Binding}" FontSize="11"/>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                            
                                            <!-- Deadline Time -->
                                            <TextBlock Grid.Column="1" 
                                                      Text="{Binding DeadlineTime, StringFormat='⏰ {0:hh\\:mm}'}" 
                                                      FontSize="12"
                                                      FontWeight="SemiBold"
                                                      Foreground="#FF9800"
                                                      VerticalAlignment="Center"/>
                                        </Grid>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
